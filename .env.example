# Datenbank-Verbindungseinstellungen
# Für lokale Entwicklung
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/besucherdb
PGHOST=localhost
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=besucherdb
PGPORT=5432

# Für Docker-Umgebung
# DATABASE_URL=postgresql://postgres:postgres@db:5432/besucherdb
# PGHOST=db

# Session Secret für Authentifizierung (sollte eine lange, zufällige Zeichenfolge sein)
SESSION_SECRET=change_this_to_a_random_string

# Server-Einstellungen
PORT=5000
NODE_ENV=development
# NODE_ENV=production # Für Produktionsumgebung
