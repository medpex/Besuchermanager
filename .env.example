# Datenbank-Verbindungseinstellungen
DATABASE_URL=postgres://visitor_app:password@localhost:5432/visitor_tracking
PGHOST=localhost
PGUSER=visitor_app
PGPASSWORD=your_password_here
PGDATABASE=visitor_tracking
PGPORT=5432

# Session Secret für Authentifizierung (sollte eine lange, zufällige Zeichenfolge sein)
SESSION_SECRET=change_this_to_a_random_string

# Server-Einstellungen (optional)
PORT=5000
NODE_ENV=production
